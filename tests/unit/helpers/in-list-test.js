
import { inList } from 'test-js/helpers/in-list';
import { module, test } from 'qunit';

module('Unit | Helper | in list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inList([42]);
  assert.ok(result);
});

